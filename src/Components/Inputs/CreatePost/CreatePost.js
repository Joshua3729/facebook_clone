import React from "react";
import classes from "./CreatePost.module.css";

const CreatePost = (props) => {
  return (
    <div className={classes.CreatePost}>
      <div className={classes.CreatePost_header}>Create Post</div>
    </div>
  );
};

export default CreatePost;
